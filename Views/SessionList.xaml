<UserControl x:Class="ExerciseTracker.Views.SessionList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExerciseTracker.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Historia Sesji Treningowych" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
        <Button Grid.Row="0" 
                Command="{Binding ShowExerciseListCommand}" 
                Content="Show exercise list" 
                Height="40" 
                Margin="0,10,0,10" 
                HorizontalAlignment="Right"/>

        <ListView Grid.Row="1" 
                  ItemsSource="{Binding Sessions}"
                  SelectedItem="{Binding SelectedSession}"
                  HorizontalContentAlignment="Stretch">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="LightGray" BorderThickness="0,0,0,1" Padding="10">
                        <TextBlock Text="{Binding Date}" FontSize="18"/>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button Grid.Row="2" 
                Command="{Binding AddNewSessionCommand}"
                Content="Dodaj nową sesję" 
                Height="40" 
                Margin="0,10,0,0"/>
    </Grid>
</UserControl>
